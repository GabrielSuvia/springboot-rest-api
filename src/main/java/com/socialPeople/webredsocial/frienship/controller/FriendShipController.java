package com.socialPeople.webredsocial.frienship.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/friendShip")
public class FriendShipController {

    // get all friendShip () //friendShip
    // get id friendShip ( String id) //:id
    // put friendShip (String friendShip) //:id
    // delete friendShip (String id) //:id
    // post friendShip () //create
}
